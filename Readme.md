Using clerk for Authentication .

Cleark will take all the user information and help in login/logout and send it to
inngest will send this information to Database and other required field

WEB HOOKS : Automated messages that are sent when something happens.



Backend (Node, Express, MongoDB, Socket.IO): auth, JWT, room model, Socket.IO signaling, room locking, chat events.

Frontend (React + Vite): register/login, lobby (create/join rooms), room UI (local + remote video, chat, participants), uses Socket.IO client + WebRTC.